const take = document.getElementById('Take');
take.addEventListener('click',function(){
    let x = prompt("Please Enter the access key");
    if(x != ""){
        alert("Sorry, but the work is in Progess");
    }
})
